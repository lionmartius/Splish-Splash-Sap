# Ignore all files by default; only R and R Markdown scripts are allowed.
*

# Keep directories traversable so scripts in any folder can be tracked.
!*/

# Allow R and R Markdown scripts, regardless of capitalization.
!*.r
!*.R
!*.rmd
!*.Rmd

# Keep the repository ignore policy under version control.
!.gitignore

.DS_Store