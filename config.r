# In this file global variables are defined, where with global
# it is meant the variables that are needed in the whole code and 
# are always constant, for instance paths or general parameters;
# these variables are written in capital letters, so they are easily findible
# In order to import them into your script, add `source("config.R")`
# at the beginning

# install and load
source("packages.R")

DATA_PATH <- "Data/"
